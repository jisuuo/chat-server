server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chatserver
    username: chatserver
    password: chatserver
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
  data:
    redis:
      host: localhost
      port: 6379
